---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION, COMPANY_NAME, COMPANY_TAGLINE, SERVICES } from '../consts';
import { getTranslation, getCurrentLanguage } from '../i18n';

// Get current language (this will be overridden by client-side)
const currentLang = 'en'; // Default for SSR
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<link rel="stylesheet" href="/src/styles/global.css" />
	</head>
	<body>
		<Header />
		
		<!-- Hero Section with Parallax -->
		<section class="hero parallax-container" id="hero">
			<div class="parallax-layer parallax-back">
				<div class="animated-bg"></div>
			</div>
			<div class="parallax-layer parallax-base">
				<div class="container">
					<div class="hero-content">
						<div class="wave-icon-large animate-bounce">🌊</div>
						<h1 class="hero-title">{COMPANY_NAME}</h1>
						<p class="tagline" data-translate="hero.tagline">{COMPANY_TAGLINE}</p>
						<p class="hero-description" data-translate="hero.description">
							Transform your digital presence with our comprehensive social media management, 
							advertising solutions, and expert mentorship. We help businesses ride the wave 
							of digital success.
						</p>
						<div class="hero-buttons">
							<a href="/contact" class="btn btn-primary" data-translate="hero.getStarted">Get Started</a>
							<a href="/about" class="btn btn-secondary" data-translate="hero.learnMore">Learn More</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Services Section -->
		<section class="section bg-light">
			<div class="container">
				<div class="text-center mb-lg animate-on-scroll">
					<h2 class="section-title" data-translate="services.title">Our Services</h2>
					<p class="section-subtitle" data-translate="services.subtitle">Comprehensive digital marketing solutions tailored to your business needs</p>
				</div>
				<div class="grid grid-3">
					{SERVICES.map((service, index) => (
						<div class={`card service-card wave-animation animate-on-scroll stagger-${(index % 6) + 1}`}>
							<div class="service-icon">
								{service === 'Social Media Management' && '📱'}
								{service === 'Digital Advertising' && '📊'}
								{service === 'Business Mentorship' && '🎯'}
								{service === 'Content Creation' && '✍️'}
								{service === 'Brand Strategy' && '🎨'}
								{service === 'Analytics & Reporting' && '📈'}
							</div>
							<h3>{service}</h3>
							<p>Professional {service.toLowerCase()} services to help your business grow and succeed in the digital landscape.</p>
						</div>
					))}
				</div>
			</div>
		</section>

		<!-- About Preview Section with Parallax -->
		<section class="section about-preview-section">
			<div class="parallax-container">
				<div class="parallax-layer parallax-back">
					<div class="floating-elements">
						<div class="floating-circle circle-1"></div>
						<div class="floating-circle circle-2"></div>
						<div class="floating-circle circle-3"></div>
					</div>
				</div>
				<div class="parallax-layer parallax-base">
					<div class="container">
						<div class="grid grid-2 about-preview">
							<div class="about-content animate-on-scroll">
								<h2 class="section-title" data-translate="about.title">Meet Our Leaders</h2>
								<p data-translate="about.description">
									PR Wave is led by two passionate digital marketing experts with years of experience 
									in social media management, advertising, and business growth strategies.
								</p>
								<p data-translate="about.experience">
									Our founders bring together decades of combined experience in helping businesses 
									establish a strong digital presence and achieve their marketing goals.
								</p>
								<a href="/about" class="btn btn-primary mt-md" data-translate="about.learnMore">Learn About Our Team</a>
							</div>
							<div class="leaders-preview animate-on-scroll">
								<div class="leader-card animate-float">
									<div class="leader-avatar">👩‍💼</div>
									<h4>Sarah Johnson</h4>
									<p>Co-Founder & Digital Strategy Expert</p>
								</div>
								<div class="leader-card animate-float" style="animation-delay: 1s;">
									<div class="leader-avatar">👨‍💼</div>
									<h4>Michael Chen</h4>
									<p>Co-Founder & Social Media Specialist</p>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Courses Section -->
		<section class="section bg-light">
			<div class="container">
				<div class="text-center mb-lg animate-on-scroll">
					<h2 class="section-title" data-translate="courses.title">Master Social Media</h2>
					<p class="section-subtitle" data-translate="courses.subtitle">Learn the ins and outs of social media platforms with our comprehensive courses</p>
				</div>
				<div class="grid grid-2">
					<div class="card course-card wave-animation animate-on-scroll stagger-1">
						<h3>Social Media Fundamentals</h3>
						<p>Master the basics of social media marketing across all major platforms.</p>
						<ul class="course-features">
							<li>Platform-specific strategies</li>
							<li>Content creation techniques</li>
							<li>Audience engagement methods</li>
							<li>Analytics and optimization</li>
						</ul>
						<a href="/courses" class="btn btn-primary mt-md">View Courses</a>
					</div>
					<div class="card course-card wave-animation animate-on-scroll stagger-2">
						<h3>Advanced Advertising</h3>
						<p>Take your advertising campaigns to the next level with advanced techniques.</p>
						<ul class="course-features">
							<li>Advanced targeting strategies</li>
							<li>Campaign optimization</li>
							<li>ROI maximization</li>
							<li>Creative development</li>
						</ul>
						<a href="/courses" class="btn btn-primary mt-md">View Courses</a>
					</div>
				</div>
			</div>
		</section>

		<!-- CTA Section with Animated Background -->
		<section class="section cta-section">
			<div class="animated-wave-bg"></div>
			<div class="container text-center">
				<h2 class="section-title animate-on-scroll" data-translate="cta.title">Ready to Ride the Wave?</h2>
				<p class="section-subtitle animate-on-scroll" data-translate="cta.subtitle">Join hundreds of businesses that have transformed their digital presence with PR Wave</p>
				<div class="cta-buttons animate-on-scroll">
					<a href="/contact" class="btn btn-primary" data-translate="cta.startJourney">Start Your Journey</a>
					<a href="/blog" class="btn btn-secondary" data-translate="cta.readBlog">Read Our Blog</a>
				</div>
			</div>
		</section>

		<Footer />
	</body>
</html>

<style>
	/* Enhanced Hero Section */
	.hero {
		background: var(--wave-gradient);
		color: var(--white);
		text-align: center;
		min-height: 100vh;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		overflow: hidden;
		perspective: 1px;
	}

	.animated-bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 200%;
		height: 200%;
		background: 
			radial-gradient(circle at 20% 50%, rgba(255,255,255,0.1) 0%, transparent 50%),
			radial-gradient(circle at 80% 20%, rgba(255,255,255,0.1) 0%, transparent 50%),
			radial-gradient(circle at 40% 80%, rgba(255,255,255,0.08) 0%, transparent 50%);
		animation: drift 30s ease-in-out infinite alternate;
	}

	.animated-bg::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: 
			radial-gradient(2px 2px at 20px 30px, rgba(255,255,255,0.3), transparent),
			radial-gradient(2px 2px at 40px 70px, rgba(255,255,255,0.2), transparent),
			radial-gradient(1px 1px at 90px 40px, rgba(255,255,255,0.4), transparent),
			radial-gradient(1px 1px at 130px 80px, rgba(255,255,255,0.3), transparent),
			radial-gradient(2px 2px at 160px 30px, rgba(255,255,255,0.2), transparent);
		background-repeat: repeat;
		background-size: 200px 100px;
		animation: sparkle 4s linear infinite;
	}

	@keyframes sparkle {
		0% { 
			opacity: 0.3;
			transform: translateY(0px);
		}
		50% { 
			opacity: 0.8;
			transform: translateY(-10px);
		}
		100% { 
			opacity: 0.3;
			transform: translateY(0px);
		}
	}

	@keyframes drift {
		0% { transform: translate(-10%, -10%) rotate(0deg); }
		100% { transform: translate(-5%, -5%) rotate(5deg); }
	}

	.hero-content {
		max-width: 800px;
		margin: 0 auto;
		position: relative;
		z-index: 2;
		padding: var(--spacing-xl) var(--spacing-md);
	}

	.wave-icon-large {
		font-size: 4rem;
		margin-bottom: var(--spacing-md);
		display: inline-block;
	}

	.animate-bounce {
		animation: customBounce 3s ease-in-out infinite;
	}

	@keyframes customBounce {
		0%, 20%, 50%, 80%, 100% { transform: translateY(0) scale(1); }
		40% { transform: translateY(-20px) scale(1.1); }
		60% { transform: translateY(-10px) scale(1.05); }
	}

	.hero-title {
		color: var(--white);
		font-size: 4rem;
		font-weight: 800;
		margin-bottom: var(--spacing-sm);
		animation: slideInUp 1s ease-out;
		text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
		letter-spacing: -0.02em;
		line-height: 1.1;
	}

	@keyframes slideInUp {
		from {
			opacity: 0;
			transform: translateY(50px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	.tagline {
		font-size: 1.8rem;
		font-weight: 400;
		margin-bottom: var(--spacing-md);
		opacity: 0.95;
		animation: slideInUp 1s ease-out 0.3s both;
		letter-spacing: 0.01em;
		line-height: 1.4;
	}

	.hero-description {
		font-size: 1.2rem;
		margin-bottom: var(--spacing-xl);
		opacity: 0.9;
		line-height: 1.7;
		animation: slideInUp 1s ease-out 0.6s both;
	}

	.hero-buttons {
		display: flex;
		gap: var(--spacing-md);
		justify-content: center;
		flex-wrap: wrap;
		animation: slideInUp 1s ease-out 0.9s both;
	}

	/* Floating Elements */
	.floating-elements {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		pointer-events: none;
	}

	.floating-circle {
		position: absolute;
		border-radius: 50%;
		background: rgba(0, 119, 190, 0.1);
		animation: floatCircle 20s linear infinite;
	}

	.circle-1 {
		width: 100px;
		height: 100px;
		top: 20%;
		left: 10%;
		animation-duration: 25s;
	}

	.circle-2 {
		width: 60px;
		height: 60px;
		top: 60%;
		right: 15%;
		animation-duration: 30s;
		animation-direction: reverse;
	}

	.circle-3 {
		width: 80px;
		height: 80px;
		bottom: 20%;
		left: 20%;
		animation-duration: 35s;
	}

	@keyframes floatCircle {
		0% { transform: translateY(0) translateX(0) rotate(0deg); }
		33% { transform: translateY(-30px) translateX(30px) rotate(120deg); }
		66% { transform: translateY(20px) translateX(-20px) rotate(240deg); }
		100% { transform: translateY(0) translateX(0) rotate(360deg); }
	}

	/* Enhanced Service Cards */
	.service-card {
		text-align: center;
		transition: all var(--transition-normal);
		position: relative;
		overflow: hidden;
	}

	.service-card:hover {
		transform: translateY(-8px) scale(1.02);
	}

	.service-icon {
		font-size: 3.5rem;
		margin-bottom: var(--spacing-md);
		display: inline-block;
		transition: transform var(--transition-normal);
	}

	.service-card:hover .service-icon {
		transform: scale(1.2) rotate(5deg);
		animation: pulse 0.6s ease-in-out;
	}

	@keyframes pulse {
		0% { transform: scale(1.2) rotate(5deg); }
		50% { transform: scale(1.3) rotate(8deg); }
		100% { transform: scale(1.2) rotate(5deg); }
	}

	/* Enhanced Leader Cards */
	.about-preview-section {
		position: relative;
		overflow: visible;
		padding: var(--spacing-xl) 0;
	}

	.about-preview-section .parallax-container {
		position: relative;
		transform-style: preserve-3d;
		min-height: 600px;
	}

	.about-preview {
		align-items: center;
		gap: var(--spacing-xl);
		position: relative;
		z-index: 2;
		padding: var(--spacing-lg) 0;
	}

	.leaders-preview {
		display: flex;
		gap: var(--spacing-md);
		justify-content: center;
	}

	.leader-card {
		text-align: center;
		padding: var(--spacing-md);
		transition: transform var(--transition-normal);
	}

	.animate-float {
		animation: gentleFloat 6s ease-in-out infinite;
	}

	@keyframes gentleFloat {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-10px); }
	}

	.leader-avatar {
		font-size: 4rem;
		margin-bottom: var(--spacing-sm);
		transition: transform var(--transition-normal);
	}

	.leader-card:hover .leader-avatar {
		transform: scale(1.1);
	}

	.leader-card h4 {
		margin-bottom: var(--spacing-xs);
		color: var(--text-heading);
	}

	.leader-card p {
		color: var(--text-secondary);
		font-size: 0.9rem;
	}

	/* Course Cards */
	.course-card {
		position: relative;
		transition: all var(--transition-normal);
	}

	.course-features {
		list-style: none;
		padding: 0;
		margin: var(--spacing-md) 0;
	}

	.course-features li {
		padding: var(--spacing-xs) 0;
		position: relative;
		padding-left: var(--spacing-md);
	}

	.course-features li::before {
		content: '✓';
		position: absolute;
		left: 0;
		color: var(--ocean-blue);
		font-weight: bold;
	}

	/* Enhanced CTA Section */
	.cta-section {
		background: var(--wave-gradient);
		color: var(--white);
		position: relative;
		overflow: hidden;
	}

	.animated-wave-bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 200%;
		height: 100%;
		background: 
			linear-gradient(45deg, transparent 40%, rgba(255,255,255,0.05) 50%, transparent 60%),
			linear-gradient(-45deg, transparent 40%, rgba(255,255,255,0.05) 50%, transparent 60%);
		animation: waveFlow 8s linear infinite;
	}

	@keyframes waveFlow {
		0% { transform: translateX(-50%); }
		100% { transform: translateX(0%); }
	}

	.cta-section h2 {
		color: var(--white);
		margin-bottom: var(--spacing-md);
	}

	.cta-section p {
		font-size: 1.1rem;
		margin-bottom: var(--spacing-lg);
		opacity: 0.9;
	}

	.cta-buttons {
		display: flex;
		gap: var(--spacing-md);
		justify-content: center;
		flex-wrap: wrap;
	}

	/* Enhanced Typography */
	.section-title {
		font-size: 2.5rem;
		font-weight: 700;
		color: var(--text-heading);
		margin-bottom: var(--spacing-md);
		letter-spacing: -0.02em;
		line-height: 1.2;
		position: relative;
	}

	.section-title::after {
		content: '';
		position: absolute;
		bottom: -8px;
		left: 50%;
		transform: translateX(-50%);
		width: 60px;
		height: 3px;
		background: var(--wave-gradient);
		border-radius: 2px;
	}

	.section-subtitle {
		font-size: 1.2rem;
		font-weight: 400;
		color: var(--text-secondary);
		line-height: 1.6;
		letter-spacing: 0.01em;
		max-width: 600px;
		margin: 0 auto;
	}

	.cta-section .section-title {
		color: var(--white);
	}

	.cta-section .section-subtitle {
		color: rgba(255, 255, 255, 0.9);
	}

	/* Card Title Enhancement */
	.service-card h3,
	.course-card h3 {
		font-weight: 600;
		letter-spacing: -0.01em;
		line-height: 1.3;
	}

	.leader-card h4 {
		font-weight: 600;
		letter-spacing: -0.01em;
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.section {
			padding: var(--spacing-lg) 0;
		}

		.container {
			padding: 0 var(--spacing-md);
		}

		.hero {
			min-height: 70vh;
			padding: var(--spacing-xl) 0;
		}

		.hero-content {
			padding: var(--spacing-lg) var(--spacing-md);
		}

		.hero-title {
			font-size: 2.5rem;
			margin-bottom: var(--spacing-md);
		}

		.tagline {
			font-size: 1.3rem;
			margin-bottom: var(--spacing-md);
		}

		.hero-description {
			font-size: 1rem;
			margin-bottom: var(--spacing-lg);
		}

		.wave-icon-large {
			font-size: 3rem;
			margin-bottom: var(--spacing-md);
		}

		.grid {
			gap: var(--spacing-md);
		}

		.grid-3 {
			grid-template-columns: 1fr;
		}

		.grid-2 {
			grid-template-columns: 1fr;
			gap: var(--spacing-lg);
		}

		.service-card {
			padding: var(--spacing-lg);
		}

		.service-icon {
			font-size: 3rem;
		}

		.about-preview-section {
			padding: var(--spacing-lg) 0;
		}

		.about-preview-section .parallax-container {
			min-height: auto;
		}

		.about-content {
			text-align: center;
			margin-bottom: var(--spacing-lg);
		}

		.section-title {
			font-size: 2rem;
		}

		.section-subtitle {
			font-size: 1rem;
		}

		.leaders-preview {
			flex-direction: column;
			gap: var(--spacing-lg);
			margin-top: var(--spacing-lg);
		}

		.leader-card {
			padding: var(--spacing-md);
		}

		.leader-avatar {
			font-size: 3rem;
		}

		.course-card {
			padding: var(--spacing-lg);
		}

		.hero-buttons,
		.cta-buttons {
			flex-direction: column;
			align-items: center;
			gap: var(--spacing-md);
		}

		.btn {
			width: 200px;
			padding: var(--spacing-sm) var(--spacing-lg);
		}

		.floating-circle {
			display: none; /* Hide floating elements on mobile for performance */
		}

		.parallax-layer {
			position: static !important;
		}

		.parallax-container {
			transform-style: initial !important;
			perspective: none !important;
		}

		.cta-section h2 {
			font-size: 2rem;
		}

		.cta-section p {
			font-size: 1rem;
		}
	}
</style>

<script>
	// Parallax scrolling effect
	function handleParallax() {
		const scrollTop = window.pageYOffset;
		const parallaxElements = document.querySelectorAll('.parallax-back');
		
		parallaxElements.forEach(element => {
			const speed = 0.5;
			const yPos = -(scrollTop * speed);
			(element as HTMLElement).style.transform = `translateY(${yPos}px)`;
		});
	}

	// Scroll-triggered animations
	function handleScrollAnimations() {
		const animatedElements = document.querySelectorAll('.animate-on-scroll');
		
		animatedElements.forEach(element => {
			const elementTop = element.getBoundingClientRect().top;
			const elementVisible = 150;
			
			if (elementTop < window.innerHeight - elementVisible) {
				element.classList.add('visible');
			}
		});
	}

	// Mouse movement parallax for hero section
	function handleMouseParallax(e: MouseEvent) {
		const hero = document.getElementById('hero');
		if (!hero) return;
		
		const rect = hero.getBoundingClientRect();
		const x = e.clientX - rect.left;
		const y = e.clientY - rect.top;
		
		const centerX = rect.width / 2;
		const centerY = rect.height / 2;
		
		const deltaX = (x - centerX) / centerX;
		const deltaY = (y - centerY) / centerY;
		
		const animatedBg = hero.querySelector('.animated-bg') as HTMLElement;
		if (animatedBg) {
			animatedBg.style.transform = `translate(${deltaX * 10}px, ${deltaY * 10}px)`;
		}
	}

	// Event listeners
	window.addEventListener('scroll', () => {
		handleParallax();
		handleScrollAnimations();
	});

	document.addEventListener('mousemove', handleMouseParallax);

	// Initial animation check
	handleScrollAnimations();

	// Translation functionality
	let currentLanguage = localStorage.getItem('language') || 'en';

	const translations = {
		en: {
			'hero.tagline': 'Ride the Digital Wave',
			'hero.description': 'Transform your digital presence with our comprehensive social media management, advertising solutions, and expert mentorship. We help businesses ride the wave of digital success.',
			'hero.getStarted': 'Get Started',
			'hero.learnMore': 'Learn More',
			'services.title': 'Our Services',
			'services.subtitle': 'Comprehensive digital marketing solutions tailored to your business needs',
			'about.title': 'Meet Our Leaders',
			'about.description': 'PR Wave is led by two passionate digital marketing experts with years of experience in social media management, advertising, and business growth strategies.',
			'about.experience': 'Our founders bring together decades of combined experience in helping businesses establish a strong digital presence and achieve their marketing goals.',
			'about.learnMore': 'Learn About Our Team',
			'courses.title': 'Master Social Media',
			'courses.subtitle': 'Learn the ins and outs of social media platforms with our comprehensive courses',
			'cta.title': 'Ready to Ride the Wave?',
			'cta.subtitle': 'Join hundreds of businesses that have transformed their digital presence with PR Wave',
			'cta.startJourney': 'Start Your Journey',
			'cta.readBlog': 'Read Our Blog'
		},
		ka: {
			'hero.tagline': 'დაეშვით ციფრულ ტალღას',
			'hero.description': 'გარდაქმენით თქვენი ციფრული არსებობა ჩვენი ყოვლისმომცველი სოციალური მედიის მართვით, სარეკლამო გადაწყვეტებით და ექსპერტის ხელმძღვანელობით. ჩვენ ვეხმარებით ბიზნესებს ციფრული წარმატების ტალღას დაეშვან.',
			'hero.getStarted': 'დაწყება',
			'hero.learnMore': 'მეტის შესწავლა',
			'services.title': 'ჩვენი სერვისები',
			'services.subtitle': 'ყოვლისმომცველი ციფრული მარკეტინგის გადაწყვეტები, რომლებიც მორგებულია თქვენი ბიზნესის საჭიროებებზე',
			'about.title': 'გაიცანით ჩვენი ლიდერები',
			'about.description': 'PR Wave-ს ხელმძღვანელობს ორი ვნებიანი ციფრული მარკეტინგის ექსპერტი, რომლებსაც წლების გამოცდილება აქვთ სოციალური მედიის მართვაში, რეკლამასა და ბიზნეს-ზრდის სტრატეგიებში.',
			'about.experience': 'ჩვენი დამფუძნებლები ერთად აერთიანებენ ათწლეულების გამოცდილებას ბიზნესების დახმარებაში ძლიერი ციფრული პრეზენსის დამყარებისა და მარკეტინგული მიზნების მიღწევისთვის.',
			'about.learnMore': 'გაიცანით ჩვენი გუნდი',
			'courses.title': 'დაეუფლეთ სოციალურ მედიას',
			'courses.subtitle': 'ისწავლეთ სოციალური მედიის პლატფორმების ყველა დეტალი ჩვენი ყოვლისმომცველი კურსებით',
			'cta.title': 'მზად ხართ ტალღას დაეშვათ?',
			'cta.subtitle': 'შეუერთდით ასობით ბიზნესს, რომლებმაც გარდაქმნეს თავიანთი ციფრული პრეზენსი PR Wave-თან ერთად',
			'cta.startJourney': 'დაიწყეთ თქვენი მგზავრობა',
			'cta.readBlog': 'წაიკითხეთ ჩვენი ბლოგი'
		}
	};

	function updateTranslations() {
		const elementsToTranslate = document.querySelectorAll('[data-translate]');
		
		elementsToTranslate.forEach(element => {
			const translationKey = element.getAttribute('data-translate');
			const translatedText = translations[currentLanguage as keyof typeof translations]?.[translationKey as keyof typeof translations.en];
			
			if (translatedText) {
				element.textContent = translatedText;
			}
		});
	}

	// Listen for language change events
	window.addEventListener('languagechange', (e: Event) => {
		const customEvent = e as CustomEvent;
		currentLanguage = customEvent.detail.language;
		updateTranslations();
	});

	// Initialize translations based on current language
	updateTranslations();
</script>
